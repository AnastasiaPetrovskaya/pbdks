$(document).ready(function() {
    var client_id = window.client_id ? window.client_id : null;
        var options = {
        bank_id: window.bank_id ? window.bank_id : null
    };

    getTable('/money_transations', 



});
